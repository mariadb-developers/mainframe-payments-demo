-- Placeholder. Real schema for the Postgres mainframe-proxy is authored in M3.
-- Files in this directory are mounted into Postgres's /docker-entrypoint-initdb.d/
-- on first start (executed in alphabetical order). Add schema and seed data here.
SELECT 1;
