-- Placeholder. Real analytics schema for MariaDB is authored in M3.
-- Files in this directory are mounted into MariaDB's /docker-entrypoint-initdb.d/
-- on first start (executed in alphabetical order). Add analytics schema here.
SELECT 1;
